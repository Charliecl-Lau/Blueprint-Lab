# Controlled derivative assets

This directory contains repository-owned values distilled from the approved visual inspection. The user-specific source DOCX and its Downloads path are intentionally not retained. `palette.json` is the canonical color input available to authoring jobs.
