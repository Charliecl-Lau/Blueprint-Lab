{
	admin off
	auto_https off
}

:{$PORT:3000} {
	handle /health {
		respond "ok" 200
	}

	handle_path /api/* {
		reverse_proxy {$API_UPSTREAM:blueprint-lab.railway.internal:8000}
	}

	handle {
		root * /srv
		encode zstd gzip
		try_files {path} /index.html
		file_server
	}
}
